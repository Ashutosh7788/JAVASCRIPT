const message = 'Hello world'
const faltuMessage = '     Hi, I am Ashutosh.    '
const finalMessage = faltuMessage.trim()
const finalMessageLowerCase = faltuMessage.trim().toLocalLowerCase()
const replaceMessage = finalMessage.replace('Hi', 'Hello')


const lastFourDigits = '3996'
const maskedAccountNumber = lastFoundDigits.padStart(16,'*')


const capitalMessage = message.toUpperCase()
const bankBalance = 45855


const concatenatedStirng = `Last four digits of my account number is`.concat('',lastFourDigits)
const templateStirng =`My account number is ${lastFourDigits}.`
const templateStirng2 =`I have ₹${bankBalance} in my account.`


const addedString = 'I have ₹' + bankBalance + 'in my account.'